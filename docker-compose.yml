
📌 Simples, limpo e sem exageros (isso é bom).

---

## 🧱 PASSO 2.6 — docker-compose.yml (placeholder)

Por enquanto, **não vamos configurar serviços**. Apenas deixe claro que virá depois:

```yaml
version: "3.9"

services:
  placeholder:
    image: alpine
    command: ["echo", "Docker Compose será configurado nas próximas etapas"]
